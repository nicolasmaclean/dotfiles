# {{ ansible_managed }}
# Rendered by roles/hyprland into ~/.config/uwsm/env-hyprland. uwsm sources it
# only for a session whose desktop name is Hyprland, after ./env. HYPR* and
# AQ_* variables belong here rather than in env.

export HYPRCURSOR_SIZE=24
